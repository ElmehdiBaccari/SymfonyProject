{% extends '@adem/Default/dashboard.html.twig' %}

{% block title %}
    Modification d'une Annonce
{% endblock %}

{% block body %}
    {{ form_start(formuser) }}
<div class="page-content-wrapper ">

    <div class="container ">
    <div class="row">
        <div class="col-md-6">
            <h1><b>  Modification d'une Annonce</b></h1>
        </div>
        <div class="col-md-6 text-right">
            <br>
            <button type="submit" class="btn btn-success" value="Create">
                <i class="fa fa-check" aria-hidden="true"></i>Confirmer
            </button>
            <a href="{{ path('list') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>Retour à la liste</a>
            <br>
        </div>
    </div>
</div>
</div>

    {% form_theme formuser '@adem/bootstrap_3_layout.html.twig' %}
    {{ form_widget(formuser) }}
    {{ form_end(formuser) }}

{% endblock %}
