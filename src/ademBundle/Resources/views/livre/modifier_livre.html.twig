{% extends '@adem/Default/dashboard.html.twig' %}

{% block title %}
    Modification du livre
{% endblock %}

{% block body %}
    {{ form_start(formlivre) }}

    <div class="row">
        <div class="col-md-6">
            <h1><b>  Modification d'un livre</b></h1>
        </div>
        <div class="col-md-6 text-right">
            <br>
            <button type="submit" class="btn btn-success" value="Create">
                <i class="fa fa-check" aria-hidden="true"></i>Confirmer
            </button>
            <a href="{{ path('afficher_livre') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>Retour à la liste</a>
            <br>
        </div>
    </div>

    {% form_theme formlivre '@adem/bootstrap_3_layout.html.twig' %}
    {{ form_widget(formlivre) }}
    {{ form_end(formlivre) }}

{% endblock %}
