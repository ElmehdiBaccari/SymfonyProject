d<table id="ProduitTab" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
    <tr>
        <th>titreLivre</th>
        <th>dateEdition</th>
        <th>descriptionLivre</th>
        <th>qteLivre</th>
        <th>categorieLivre</th>
        <th>image</th>
        <th>ay7aja</th>

    </tr>
    </thead>
    <thead>
    <tbody>
    {% for p in livres %}
        <tr>
            <td>{{ p.titreLivre }}</td>
            <td>{{ p.dateEdition |date("m/d/Y")}}</td>
            <td>{{ p.descriptionLivre }} </td>
            <td>{{ p.qteLivre }}</td>
            <td>{{ p.categorieLivre }}</td>
            <td style="width: 60px; padding:0px; text-align: center">
                {% if p.image != '' %}
                    <img alt="Livre Pic" style="width:150px;"
                         src="http:\\localhost\web_projet\web\Uploads\livre\{{ p.image}}"
                         class="img-circle img-responsive">

                {% endif %}

            </td>


        </tr>
    {% endfor %}
    </tbody>
</table>