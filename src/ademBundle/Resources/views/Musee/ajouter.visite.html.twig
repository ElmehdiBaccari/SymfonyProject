{% extends '@adem/Default/front.html.twig' %}

{% block title %}
    Ajout d'annonce
{% endblock %}
{% block javascripts %}
    <script>
        $("#adembundle_collection_imageFile_file").change(function() {

            if (this.files && this.files[0]) {
                var reader = new FileReader();

                reader.onload = function(e) {
                    $('#image').attr('src', e.target.result);
                    $('#image').css('display','inline-block');
                }

                reader.readAsDataURL(this.files[0]);
            }

        });
    </script>
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-6">
                    <h1>Programmation d'une visite</h1>
                </div>
                <div class="col-md-6 text-right"><br>

                    <br>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="card" style="padding: 25px">
                        {#<ul class="nav nav-tabs nav-tabs-simple hidden-sm-down" role="tablist"#}
                        {#data-init-reponsive-tabs="dropdownfx">#}
                        {#<li class="nav-item">#}
                        {#<a class="active" data-toggle="tab" role="tab" data-target="#annonce" href="#"#}
                        {#aria-expanded="true">Annonce</a>#}
                        {#</li>#}
                        {#</ul>#}
                        {#<div class="tab-content">#}
                        {#<div class="tab-pane active" id="annonce" aria-expanded="true">#}

                        {{ form_start(formMusee) }}
                        {% form_theme formMusee '@adem/bootstrap_3_layout.html.twig' %}







                        <div>
                            <img src="" id="image" style="display: none" width="200px"height="200px">
                        </div>


                        {#</div>#}
                    </div>
                </div>
                <div>
                    <img src="" id="image" style="display: none" width="200px"height="200px">
                </div>

                {{ form_row(formMusee.arrive) }}
                {{ form_row(formMusee.personne) }}

                <h4>Prix total :</h4>

                <h5 ><span id="price"> 0 </span>dt</h5>

                <button type="submit" class="btn btn-success" value="Create">
                    <i class="fa fa-check" aria-hidden="true"></i> Confirmer
                </button>
                {{ form_end(formMusee) }}
            </div>
        </div>
    </div>
    <script>
        document.getElementById("adembundle_musee_personne").addEventListener("keyup", myFunction);

        function myFunction() {


            var element = document.getElementById("price");
            if (document.getElementById("adembundle_musee_personne").value!='')
            element.innerHTML = parseInt(document.getElementById("adembundle_musee_personne").value)*15;
            else
                element.innerHTML =0;
        }


    </script>
{% endblock %}