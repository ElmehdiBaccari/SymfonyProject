{% extends '@adem/Default/dashboard.html.twig' %}

{% block title %}
    Modification d'une Annonce
{% endblock %}

{% block body %}
    {{ form_start(formsalle) }}

    <div class="row">
        <div class="col-md-6">
            <h1><b>  Modification d'une Annonce</b></h1>
        </div>
        <div class="col-md-6 text-right">
            <br>
            <button type="submit" class="btn btn-success" value="Create">
                <i class="fa fa-check" aria-hidden="true"></i>Confirmer
            </button>
            <a href="{{ path('afficher_salle') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>Retour à la liste</a>
            <br>
        </div>
    </div>

    {% form_theme formsalle '@adem/bootstrap_3_layout.html.twig' %}
    {{ form_widget(formsalle) }}
    {{ form_end(formsalle) }}

{% endblock %}
